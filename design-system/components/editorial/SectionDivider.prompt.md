Centered mono section label flanked by hairlines. Separates long-form article sections.

```jsx
<SectionDivider label="III — Shared Elements" />
```

Roman numerals + em-dash + title is the house convention for `label`.
