The gold ✦ flourish used above heroes and inside verdict headings. Decorative, ecclesiastical.

```jsx
<Ornament />
<Ornament count={3} onDark />
```

`count` glyphs (default 3), customizable `glyph` and `spacing`. Set `onDark` over maroon surfaces.
